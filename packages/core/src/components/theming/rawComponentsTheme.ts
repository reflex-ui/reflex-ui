import { rawButtonTheme } from '../buttons/theming/rawButtonTheme';
import { rawIconWrapperTheme } from '../icons/theming/rawIconWrapperTheme';
import { rawTypographyTheme } from '../typography/theming/rawTypographyTheme';
import { ComponentsTheme } from './ComponentsTheme';

export const rawComponentsTheme: ComponentsTheme = {
  button: rawButtonTheme,
  icon: rawIconWrapperTheme,
  typography: rawTypographyTheme,
};
