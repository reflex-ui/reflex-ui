import {
  TouchableWithoutFeedback,
  TouchableWithoutFeedbackProps,
} from 'react-native';
import { Touchable } from '../../';

export const DefaultButtonTouchable: Touchable<
  TouchableWithoutFeedbackProps
> = TouchableWithoutFeedback;
